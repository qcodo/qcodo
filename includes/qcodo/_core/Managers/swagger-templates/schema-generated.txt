<?php
	namespace %s\Models\Schema;
	use QJsonBaseClass;
	use QDateTime;
	use stdClass;
	use %s\Models\Database;

	/**
	 * @class %s%s
	 * @extends QJsonBaseClass
%s
	 */
	class %s%s extends QJsonBaseClass {
		protected static $_Model = array(
%s
		);
%s
		/**
		 * Returns an instance of %s%s - can either the instance that is passed in, or if an array-map, it will create a new instance and apply the properties from the array-map, or if null it will create a new empty instance
		 * @param %s%s|array|null $mixed
		 * @return %s%s
		 */
		public static function GetInstance(%s%s|array|null $mixed) {
			if (is_array($mixed)) return self::JsonDecodeForClass(get_called_class(), json_decode(json_encode($mixed)));
			if (!$mixed) return new %s%s();
			return $mixed;
		}

		/**
		 * Creates a %s%s from a JSON string or JSON-decoded stdClass object
		 *
		 * @param mixed $json can be a string-based JSON or a stdClass
		 * @return %s%s
		 * @throws
		 */
		public static function JsonDecode($json) {
			return self::JsonDecodeForClass(get_called_class(), $json);
		}

		/**
		 * Creates an array of %s%s from a JSON string or an array of JSON-decoded stdClass objects
		 *
		 * @param mixed $json can be a string-based JSON or an array of stdClass
		 * @return %s%s[]
		 * @throws
		 */
		public static function JsonDecodeArray($json) {
			return self::JsonDecodeArrayForType(get_called_class(), $json);
		}

%s

/*
		/**
		 *
		 * @return Schema\%s%s
		 *
		public function GetSchema() {
			$%s = new Schema\%s%s();%s

%s

			return $%s;
		}

		/**
		 * @param Schema\%s%s $%s
		 *
		protected function UpdateFromSchema(Schema\%s%s $%s) {
%s

			$this->Save();
		}
*/
	}
